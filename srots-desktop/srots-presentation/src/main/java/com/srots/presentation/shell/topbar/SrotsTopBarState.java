package com.srots.presentation.shell.topbar;

/**
 * Explicit TopBar interaction state (presentation only).
 */
public enum SrotsTopBarState {
    READY,
    SEARCHING,
    NOTIFICATION_OPEN,
    PROFILE_OPEN,
    OFFLINE
}
