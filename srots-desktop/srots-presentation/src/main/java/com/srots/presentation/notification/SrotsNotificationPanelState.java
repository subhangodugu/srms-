package com.srots.presentation.notification;

/**
 * Panel load / sync presentation state.
 */
public enum SrotsNotificationPanelState {
    IDLE,
    LOADING,
    READY,
    EMPTY,
    ERROR,
    OPEN,
    CLOSED
}
