package com.srots.presentation.shell.statusbar;

/**
 * User-facing activity kinds (presentation only).
 */
public enum ApplicationActivityType {
    IDLE,
    WORKING,
    SYNCING,
    SAVING,
    LOADING,
    IMPORTING,
    EXPORTING,
    DEPLOY,
    BUILD,
    PROCESS,
    ERROR
}
