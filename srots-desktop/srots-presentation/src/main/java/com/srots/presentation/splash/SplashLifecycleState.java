package com.srots.presentation.splash;

/**
 * Splash / startup presentation lifecycle.
 */
public enum SplashLifecycleState {
    CREATED,
    VISIBLE,
    INITIALIZING,
    READY,
    CLOSING,
    CLOSED,
    FAILED
}
