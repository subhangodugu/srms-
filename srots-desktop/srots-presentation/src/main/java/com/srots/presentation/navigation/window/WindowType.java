package com.srots.presentation.navigation.window;

/**
 * Desktop window kinds. Default navigation targets the primary application window.
 */
public enum WindowType {
    PRIMARY_WINDOW,
    MODAL_DIALOG,
    NON_MODAL_DIALOG,
    DETAIL_WINDOW
}
